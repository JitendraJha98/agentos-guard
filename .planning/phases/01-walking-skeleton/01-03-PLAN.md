---
phase: 01-walking-skeleton
plan: 03
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/pipeline/pyproject.toml
  - packages/pipeline/src/agentos_pipeline/__init__.py
  - packages/pipeline/src/agentos_pipeline/risk/__init__.py
  - packages/pipeline/src/agentos_pipeline/risk/normalize.py
  - packages/pipeline/src/agentos_pipeline/risk/prompt_injection.py
  - packages/pipeline/src/agentos_pipeline/risk/aggregator.py
  - tests/unit/test_detector_recall.py
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "The detector fires (matched pattern ID + risk_score >= 0.4) on every covered probe class: instruction-override, role-hijack, exfil-directive."
    - "Benign content containing trigger-adjacent words scores below 0.4 and is not flagged."
    - "Obfuscated variants (zero-width, NFKC homoglyph, base64) of a covered probe still fire after normalize()."
    - "The same AgentAction yields a byte-identical RiskFinding across 100 runs (deterministic, no RNG/wall-clock)."
    - "score() is pure CPU — no network or model import is reachable from it; input is bounded to 32 KB."
  artifacts:
    - path: "packages/pipeline/src/agentos_pipeline/risk/prompt_injection.py"
      provides: "PromptInjectionScorer (inline=True, deterministic regex, ReDoS-safe)"
      contains: "class PromptInjectionScorer"
    - path: "packages/pipeline/src/agentos_pipeline/risk/normalize.py"
      provides: "NFKC + zero-width strip + opportunistic base64 decode before matching"
      contains: "NFKC"
    - path: "packages/pipeline/src/agentos_pipeline/risk/aggregator.py"
      provides: "assess_risk runs inline scorers, max-pool risk_score, no I/O"
      contains: "def assess_risk"
  key_links:
    - from: "PromptInjectionScorer"
      to: "agentos_contract.RiskFinding / RiskScorer"
      via: "implements the RiskScorer Protocol; returns typed RiskFinding"
      pattern: "RiskFinding"
    - from: "assess_risk"
      to: "normalize"
      via: "normalize before matching (evasion mitigation)"
      pattern: "normalize"
---

<objective>
Build the SEC-01 risk stage: the deterministic in-house `PromptInjectionScorer` (stdlib `re` + Pydantic typed `RiskFinding`) behind the `RiskScorer` Protocol, the `normalize()` de-obfuscation pass, and the `assess_risk` aggregator. This is the AI-bearing component of Phase 1 — and it is deterministic by design so the D-04 CI gate stays non-flaky.

Purpose: SEC-01 contributes a typed prompt-injection finding to `risk_score`. It is advisory only — it can never relax the opa-wasm policy floor (that invariant lives in the graduated stage, plan 01-05). The detector MUST be pure CPU, sub-ms, no LLM/model on the hot path (P0-killer), ReDoS-safe, and byte-identical across runs.
Output: `pipeline` package (created here) with the `risk/` subpackage and its recall/precision/evasion/determinism/purity tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-walking-skeleton/SKELETON.md
@.planning/phases/01-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-walking-skeleton/01-RESEARCH.md
@.planning/phases/01-walking-skeleton/01-AI-SPEC.md
@.planning/phases/01-walking-skeleton/01-VALIDATION.md
@docs/architecture/05-security-and-runtime.md
@CLAUDE.md

<interfaces>
<!-- Produced by plan 01-01 (contract); CONSUMED here. Do not redefine. -->
From agentos_contract: RiskFinding(scorer, category, risk_score, matched, detail, inline) [frozen, extra=forbid,
  matched validator rejects raw payload]; RiskScorer Protocol(name, inline, score(action)->RiskFinding); AgentAction.

<!-- Shapes to CREATE (AI-SPEC §3/§4 is the verbatim source). -->
normalize.py:  normalize(text: str) -> str   # NFKC fold + strip zero-width/bidi (U+200B..U+200F,U+202A..U+202E,U+2060,U+FEFF) + opportunistic base64 decode appended
prompt_injection.py:
  PromptInjectionScorer: name="prompt_injection.v1", inline=True; patterns _OVERRIDE/_ROLE_HIJACK/_EXFIL compiled ONCE at construction (bounded quantifiers, ReDoS-safe); score(action)->RiskFinding;
    matched in {instruction_override, role_hijack, exfil_directive}; score = 0 if no match else min(1.0, 0.4+0.3*len(matched)); inspect tool args + fetched content; cap inspected text at 32 KB and note truncation in detail.
aggregator.py:  assess_risk(action, scorers: list[RiskScorer]) -> tuple[float, list[RiskFinding]]   # runs inline scorers only, max-pool risk_score, NO I/O
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SEC-01 deterministic prompt-injection detector (RED then GREEN)</name>
  <files>packages/pipeline/pyproject.toml, packages/pipeline/src/agentos_pipeline/__init__.py, packages/pipeline/src/agentos_pipeline/risk/__init__.py, packages/pipeline/src/agentos_pipeline/risk/normalize.py, packages/pipeline/src/agentos_pipeline/risk/prompt_injection.py, packages/pipeline/src/agentos_pipeline/risk/aggregator.py, tests/unit/test_detector_recall.py</files>
  <read_first>
    - .planning/phases/01-walking-skeleton/01-AI-SPEC.md §3 "Entry Point Pattern" (the PromptInjectionScorer code, verbatim) and §4 "Implementation Guidance" (normalize.py + aggregator.py code, bounded-input 32 KB strategy) and §5 "Reference Dataset" (the 8 attack + 8 benign probe composition + the xfail known-gaps)
    - .planning/phases/01-walking-skeleton/01-RESEARCH.md § "Risk Stage (SEC-01)" (the key points to preserve) and § "Common Pitfalls" Pitfall 2 (compile once), Pitfall 5 (determinism)
    - .planning/phases/01-walking-skeleton/01-CONTEXT.md (D-12 one heuristic, deterministic; LLM interpreter NOT in Phase 1)
    - agentos_contract.risk (the RiskScorer Protocol + RiskFinding the scorer must satisfy)
  </read_first>
  <behavior>
    - RED: `tests/unit/test_detector_recall.py` parametrized over the AI-SPEC §5 labeled probe set BEFORE the scorer exists.
    - Recall (D1): each of the 8 attack probes (instruction-override incl. one typoglycemia variant, role-hijack, exfil-directive incl. the canonical indirect-injection-via-fetched-page-content probe, and the obfuscation variants) yields a RiskFinding with the expected `matched` pattern ID and `risk_score >= 0.4`. Recall on covered classes = 100%.
    - Precision (D2): each of the 8 benign samples (legitimate fetches + content with trigger-adjacent words like "ignore the noise", "system requirements", "the token bucket algorithm") yields `risk_score < 0.4`. Zero false-fires on the benign set.
    - Evasion (D3): zero-width-injected, NFKC-foldable homoglyph, and base64-encoded variants of a covered probe still fire after normalize(). Paraphrase / novel-encoding / sharded-exfil variants are marked `pytest.mark.xfail` (documented out-of-Phase-1, deferred to Phase-3 classifier) so the suite never overclaims.
    - Determinism (D4): the same AgentAction yields a byte-identical RiskFinding (`model_dump_json()`) across 100 runs (`--count=100`); no wall-clock/RNG dependency.
    - Purity (D4): a static import-guard test asserts `score()`'s module graph excludes `socket`, `http`, `urllib.request`, `transformers`, `anthropic` (no network/model on the hot path).
    - Bounded input (D4): a >32 KB hostile page is truncated deterministically and the finding `detail` records that truncation occurred.
    - GREEN: implement normalize/prompt_injection/aggregator so all non-xfail tests pass; patterns compiled ONCE at construction (assert no per-call `re.compile`).
  </behavior>
  <action>
    Create `packages/pipeline/pyproject.toml` (`agentos-pipeline`) depending ONLY on agentos-contract (stdlib `re`/`unicodedata`/`base64` are free; NO transformers/anthropic/network deps). Implement `normalize.py` verbatim from AI-SPEC §4: `unicodedata.normalize("NFKC", text)`, strip the zero-width/bidi class re.compile of the character class (the smuggling codepoints U+200B-U+200F, U+202A-U+202E, U+2060, U+FEFF) written as backslash-u ESCAPE sequences (never literal invisible characters), opportunistically base64-decode `\b[A-Za-z0-9+/]{16,}={0,2}\b` matches and append decoded plaintext (never raise on bad base64). Implement `prompt_injection.py` verbatim from AI-SPEC §3: `_OVERRIDE`/`_ROLE_HIJACK`/`_EXFIL` compiled once as class attributes with BOUNDED quantifiers (`[^.\n]{0,40}` etc. — ReDoS-safe, no nested unbounded groups); `_inspect_text` joins `action.payload` values, calls `normalize()` FIRST, caps at 32 KB (record truncation in `detail`); `score` matches each pattern, builds `matched`, computes `min(1.0, 0.4 + 0.3*len(matched))`, returns a `RiskFinding(category="prompt_injection", inline=True, ...)`. Implement `aggregator.py`: `assess_risk(action, scorers)` runs only `inline` scorers, returns `(max risk_score, findings)`, performs NO I/O. The detector is stateless and pure (no accumulation across calls). Build the labeled probe set as a module-level fixture/list in the test file per AI-SPEC §5.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_detector_recall.py -x</automated>
  </verify>
  <done>normalize/prompt_injection/aggregator implemented; recall=100% on covered classes, 0 false-fires on benign, obfuscation variants caught, byte-identical findings over 100 runs, no network/model import reachable from score().</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_detector_recall.py -x` green (all non-xfail).
- `uv run pytest tests/unit/test_detector_recall.py --count=100 -q` green (determinism — byte-identical findings).
- Import-guard test green (no network/model import reachable from `score()`).
- `grep -v '^#' packages/pipeline/src/agentos_pipeline/risk/prompt_injection.py | grep -c "re.compile"` count equals the number of pattern class-attributes (compiled once at class definition, not per call).
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched content → detector | Attacker-controlled page content / tool args enter `score()`; the detector must not be evadable by trivial obfuscation, must not ReDoS, and must not be talked into "approving" anything (it only contributes risk). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering / Elevation | `PromptInjectionScorer.score` (indirect injection) | mitigate | Deterministic regex over normalized text catches the covered classes; the AUTHORITATIVE block is the policy floor (plan 01-04/01-05) — the detector only contributes risk and can never upgrade permission. Recall tested (D1). |
| T-01-09 | Tampering | obfuscated/encoded injection | mitigate | `normalize()` (NFKC + zero-width strip + base64) before matching; honest partial coverage documented via `xfail` for paraphrase/novel encodings (full coverage = Phase-3 classifier). Tested (D3). |
| T-01-10 | Denial of Service | ReDoS via hostile page | mitigate | Bounded-quantifier regex compiled once at construction; 32 KB input cap with truncation recorded. ASVS V5. Tested (D4 bounded-input). |
| T-01-11 | Information Disclosure | finding contents | mitigate | `RiskFinding.matched` holds pattern IDs only (validator from plan 01-01); raw payload never enters the finding/audit log. |
| T-01-12 | Elevation (P0-killer) | LLM/model on hot path | mitigate | Detector is stdlib-only, `inline=True`, sub-ms; import-guard test asserts no `socket`/`http`/`transformers`/`anthropic` reachable from `score()`. Tested (D4 purity). |
</threat_model>

<success_criteria>
- 100% recall on the covered probe classes; 0 false-fires on the benign set (D1/D2).
- Trivial-obfuscation variants caught after `normalize()`; out-of-scope evasion honestly xfail-marked (D3).
- Byte-identical findings across 100 runs; no network/model import reachable from `score()`; 32 KB bounded input (D4).
- The detector implements the `RiskScorer` Protocol and emits a typed `RiskFinding` (SEC-01).
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton/01-03-SUMMARY.md` when done.
</output>
