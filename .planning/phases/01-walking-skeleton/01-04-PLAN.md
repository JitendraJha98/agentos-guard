---
phase: 01-walking-skeleton
plan: 04
type: execute
wave: 3
depends_on: ["01-01"]
files_modified:
  - policies/egress.rego
  - policies/egress_test.rego
  - policies/build/.gitkeep
  - packages/pipeline/src/agentos_pipeline/policy.py
  - tests/unit/test_policy_engine.py
autonomous: false
requirements: [POL-03]
user_setup:
  - service: opa-cli
    why: "OPA CLI compiles the Rego principle to a WASM bundle (opa build -t wasm); it is a binary, not a PyPI package."
    dashboard_config:
      - task: "Install a pinned OPA CLI 1.x release locally (and in CI) so opa build/opa test are runnable"
        location: "https://github.com/open-policy-agent/opa/releases"
must_haves:
  truths:
    - "opa-wasmtime is human-verified (source repo reviewed + smoke-test loads a compiled egress.wasm) BEFORE it is installed/locked."
    - "The egress allowlist principle is authored directly as Rego and compiles to a WASM bundle with the OPA CLI."
    - "WasmPolicyEngine loads the WASM bundle ONCE at construction and returns allow for an allowlisted host, deny for a non-allowlisted host."
    - "opa test passes the Rego unit tests (allowlisted allowed, attacker denied)."
    - "Decisions honored: D-05 (opa-wasm/opa-wasmtime runs in-process behind the PolicyEngine interface) and D-06 (the WASM build is wired into CI)."
  artifacts:
    - path: "policies/egress.rego"
      provides: "the single Phase-1 principle: outbound only to allowlisted hosts (D-02)"
      contains: "data.allowlist"
    - path: "packages/pipeline/src/agentos_pipeline/policy.py"
      provides: "PolicyEngine Protocol + WasmPolicyEngine (opa-wasmtime, load-once)"
      contains: "class PolicyEngine"
    - path: "policies/egress_test.rego"
      provides: "deterministic opa test coverage"
      contains: "test_attacker_host_denied"
  key_links:
    - from: "WasmPolicyEngine"
      to: "policies/build/egress.wasm"
      via: "load WASM bundle once at construction (never per request)"
      pattern: "wasm"
    - from: "egress.rego"
      to: "data.allowlist"
      via: "allow if input.host in data.allowlist (principle in Rego; hosts as data)"
      pattern: "in data.allowlist"
---

<objective>
Establish the deterministic policy floor (POL-03): human-verify the `opa-wasmtime` substitution, author the egress-allowlist principle directly as Rego (D-02), compile it to a WASM bundle, and wrap it behind the `PolicyEngine` interface as `WasmPolicyEngine` loaded once at startup.

Purpose: This is the authoritative security control of the whole skeleton. The locked `opa-wasm` is Python-3.12-incompatible (its `wasmer` dep has no modern wheels) — the research-mandated replacement is `opa-wasmtime`, a young single-maintainer package that MUST be human-verified before it is committed (RESEARCH Assumption A1 / Package Legitimacy Audit). The `PolicyEngine` interface keeps OPA-server a future toggle and `opa-wasmtime` a one-file swap.
Output: the Rego principle + tests, the compiled WASM artifact path, and `WasmPolicyEngine` behind `PolicyEngine`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-walking-skeleton/SKELETON.md
@.planning/phases/01-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-walking-skeleton/01-RESEARCH.md
@.planning/phases/01-walking-skeleton/01-VALIDATION.md
@docs/architecture/04-constitution-and-policy.md
@docs/architecture/adr/0003-opa-rego-policy-substrate.md
@CLAUDE.md

<interfaces>
<!-- Produced by plan 01-01 (contract); CONSUMED here. -->
From agentos_contract: Outcome (allow/deny used this phase).

<!-- Shapes to CREATE (RESEARCH § "Policy Engine" is the verbatim source). -->
policy.py:
  PolicyEngine (Protocol): evaluate(input: dict) -> PolicyResult       # the toggle seam (OPA-server later)
  PolicyResult: outcome:Outcome, code:str, policy_id:str, detail:str
  WasmPolicyEngine(wasm_path: str, allowlist: list[str]): loads OPAPolicy(wasm_path) ONCE + set_data({"allowlist": allowlist});
    evaluate(input) -> PolicyResult  (input = {"host","method","type"}; allow→"egress_allowlisted", deny→"egress_allowlist_violation", policy_id="egress.allow")
policies/egress.rego:  package agentos.egress; default allow := false; allow if { input.type=="tool_call"; input.host in data.allowlist }
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify opa-wasmtime before install (RESEARCH A1)</name>
  <read_first>
    - .planning/phases/01-walking-skeleton/01-RESEARCH.md § "Standard Stack" (the opa-wasm→opa-wasmtime correction + the [ASSUMED] flag), § "Package Legitimacy Audit" (the single package needing human verification), "Assumptions Log A1", "Open Questions" Q1
    - .planning/phases/01-walking-skeleton/01-VALIDATION.md § "Manual-Only Verifications"
  </read_first>
  <files>(no production files committed this task — a throwaway smoke script only; opa-wasmtime is added to deps in Task 3 only after approval)</files>
  <action>BLOCKING human-verify checkpoint (never auto-approvable; workflow.auto_advance is ignored). Claude prepares the throwaway smoke script described in <what-built> and presents it; a human reviews the opa-wasmtime source repo and the smoke-test allow/deny output and either approves the package or selects a documented fallback. Do NOT add opa-wasmtime/wasmtime to the locked dependencies until this checkpoint is approved.</action>
  <verify>
    <automated>python scripts/smoke_opa_wasmtime.py 2>&1 | tail -5  # prints allow for api.example.com, deny for attacker.example, and the evaluate() result shape</automated>
  </verify>
  <done>A human has reviewed github.com/nickdeis/python-opa-wasmtime, the smoke script demonstrates allow/deny against a freshly compiled egress.wasm, the evaluate() result shape is recorded for Task 3, and the package is approved (or a fallback chosen).</done>
  <what-built>
    Before this checkpoint, Claude prepares (does NOT yet add to the locked deps): a throwaway smoke script that compiles `policies/egress.rego` to WASM with the pinned OPA CLI (`opa build -t wasm -e 'agentos/egress/allow' policies/egress.rego`, extract `policy.wasm`), loads it via `opa_wasmtime.OPAPolicy`, `set_data({"allowlist": ["api.example.com"]})`, and evaluates two inputs (`{"type":"tool_call","host":"api.example.com"}` → allow, `{"type":"tool_call","host":"attacker.example"}` → deny). The script also prints the exact Python object `OPAPolicy.evaluate()` returns (resolves Open Question Q1 — the result-set shape for `_extract_bool`).
  </what-built>
  <how-to-verify>
    1. Review the source repo `https://github.com/nickdeis/python-opa-wasmtime` (maintenance, issues, that it wraps the Bytecode Alliance `wasmtime` runtime and loads OPA-CLI WASM bundles).
    2. Confirm `opa-wasmtime` and `wasmtime` install on the target Python (3.12/3.13) with NO `wasmer` in the resolution.
    3. Run the smoke script; confirm it prints `allow` for `api.example.com` and `deny` for `attacker.example`, and note the printed result-set shape.
    4. Decide: APPROVE `opa-wasmtime`, or fall back to the documented alternative (OPA-server behind the same `PolicyEngine` interface, or `regopy`).
  </how-to-verify>
  <acceptance_criteria>
    - The smoke script runs and demonstrates allow/deny against a freshly compiled `egress.wasm`.
    - The exact `evaluate()` result shape is recorded (for `_extract_bool` in Task 3).
    - A human has reviewed the source repo and explicitly approved (or chosen a fallback).
  </acceptance_criteria>
  <resume-signal>Type "approved: opa-wasmtime" to proceed with the install, or "fallback: opa-server" / "fallback: regopy" to switch the engine implementation (interface unchanged).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author the egress-allowlist Rego principle + tests + compile to WASM</name>
  <files>policies/egress.rego, policies/egress_test.rego, policies/build/.gitkeep</files>
  <read_first>
    - .planning/phases/01-walking-skeleton/01-RESEARCH.md § "Policy Engine (in-process OPA WASM)" (the egress.rego + egress_test.rego + the CI build step block) and § "Architecture Patterns" Pattern 3 (compile-on-build, load once)
    - .planning/phases/01-walking-skeleton/01-CONTEXT.md (D-02 principle authored directly as Rego; D-06 WASM as a build artifact; Claude's Discretion: allowlist as Rego `data`)
  </read_first>
  <action>
    Author `policies/egress.rego` exactly as RESEARCH § Policy Engine specifies: `package agentos.egress`, `import rego.v1`, `default allow := false`, and `allow if { input.type == "tool_call"; input.host in data.allowlist }` — the principle stays in Rego; concrete hosts are supplied as a Rego `data` document at startup (so the red-team test can mutate the allowlist without recompiling). Author `policies/egress_test.rego` with `test_allowlisted_host_allowed` (host in allowlist → allow) and `test_attacker_host_denied` (attacker host → not allow), using `with input as ...` / `with data.allowlist as ...`. Add `policies/build/.gitkeep` (the compiled `egress.wasm` is a CI/build artifact, not committed; the directory must exist). Document in the plan SUMMARY the exact pinned OPA CLI command: `opa build -t wasm -e 'agentos/egress/allow' policies/egress.rego` then extract `policy.wasm` → `policies/build/egress.wasm`. Verify with `opa test policies/`.
  </action>
  <verify>
    <automated>opa test policies/</automated>
  </verify>
  <acceptance_criteria>
    - `opa test policies/` passes both Rego tests.
    - `grep -c "in data.allowlist" policies/egress.rego` returns ≥ 1 (principle expressed in Rego, hosts as data).
    - `policies/egress.rego` has `default allow := false` (deny-by-default floor).
    - `policies/build/` exists.
  </acceptance_criteria>
  <done>The single principle is authored as Rego, passes `opa test`, and compiles to a WASM bundle via the pinned OPA CLI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WasmPolicyEngine behind the PolicyEngine interface (load-once)</name>
  <files>packages/pipeline/src/agentos_pipeline/policy.py, tests/unit/test_policy_engine.py</files>
  <read_first>
    - .planning/phases/01-walking-skeleton/01-RESEARCH.md § "Policy Engine (in-process OPA WASM)" (the WasmPolicyEngine + PolicyEngine Protocol code; the result-shape note from Open Q1 resolved in Task 1) and § "Common Pitfalls" Pitfall 2 (load once, never per request)
    - .planning/phases/01-walking-skeleton/01-CONTEXT.md (D-05 PolicyEngine interface, opa-wasm togglable)
    - policies/egress.rego + the result-set shape recorded in Task 1
    - agentos_contract (Outcome)
  </read_first>
  <behavior>
    - Test: WasmPolicyEngine(wasm_path, allowlist=["api.example.com"]).evaluate({"type":"tool_call","host":"api.example.com","method":"GET"}) → PolicyResult(outcome=allow, code="egress_allowlisted", policy_id="egress.allow").
    - Test: same engine, host="attacker.example" → PolicyResult(outcome=deny, code="egress_allowlist_violation").
    - Test: the WASM bundle is loaded ONCE — constructing the engine loads `OPAPolicy` a single time; `evaluate` does not reload (assert via a load-count probe or by constructing once and evaluating many times without re-reading the file).
    - Test: `WasmPolicyEngine` satisfies the `PolicyEngine` Protocol (structural typing / isinstance check on the Protocol).
  </behavior>
  <action>
    Add `opa-wasmtime>=0.1.1` and `wasmtime>=27` to the pipeline package deps and `uv.lock` (ONLY now that Task 1 approved it). Implement `policy.py` per RESEARCH § Policy Engine: `PolicyEngine` Protocol (`evaluate(input)->PolicyResult`), `PolicyResult` dataclass/Pydantic (outcome, code, policy_id, detail), and `WasmPolicyEngine` whose `__init__(wasm_path, allowlist)` constructs `OPAPolicy(wasm_path)` ONCE and calls `set_data({"allowlist": allowlist})`; `evaluate(input)` calls `self._policy.evaluate(input)`, extracts the boolean with `_extract_bool` using the result shape recorded in Task 1, and maps to `PolicyResult` (allow→code "egress_allowlisted"; deny→code "egress_allowlist_violation", policy_id "egress.allow"). Never compile/load per request (Pitfall 2). Write `tests/unit/test_policy_engine.py` realizing the <behavior> cases (the test compiles or uses a fixture `egress.wasm`; gate on OPA CLI availability with a clear skip if absent, but the wave-merge CI step compiles it).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_policy_engine.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_policy_engine.py -x` passes (skips only if the OPA CLI / compiled wasm is genuinely unavailable in the dev env).
    - Allowlisted host → allow; attacker host → deny (covered by passing tests).
    - `grep -v '^#' packages/pipeline/src/agentos_pipeline/policy.py | grep -c "OPAPolicy("` returns exactly 1 (constructed once in `__init__`, not in `evaluate`).
    - `WasmPolicyEngine` is usable wherever `PolicyEngine` is expected (Protocol satisfied).
    - `uv.lock` now includes `opa-wasmtime` and `wasmtime` (and NOT `wasmer`).
  </acceptance_criteria>
  <done>The deterministic OPA WASM floor evaluates allow/deny behind the PolicyEngine interface, loaded once at construction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| action host → policy floor | The target host (derived from attacker-influenceable tool args) is checked against the allowlist; this is THE access-control decision. |
| package supply chain | `opa-wasmtime` is a young, single-maintainer, WebSearch-discovered package being added to the trusted dependency set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Information Disclosure | egress to non-allowlisted host | mitigate | Deny-by-default Rego allowlist (`default allow := false`); a non-allowlisted host → terminal `deny`. ASVS V4 access control. Tested via `opa test` + engine unit tests. |
| T-01-14 | Tampering | WASM load per request (Pitfall 2) | mitigate | `OPAPolicy` constructed once at startup; `grep` gate asserts a single `OPAPolicy(` construction. |
| T-01-SC | Tampering (supply chain) | `opa-wasmtime` install | mitigate (BLOCKING) | `[ASSUMED]` package gated behind the blocking human-verify checkpoint (Task 1): source-repo review + smoke-test loading a real `egress.wasm` BEFORE install. Fallbacks documented (OPA-server / regopy) behind the same `PolicyEngine` interface. This checkpoint is never auto-approvable. |
</threat_model>

<verification>
- `opa test policies/` green.
- `uv run pytest tests/unit/test_policy_engine.py -x` green.
- `uv.lock` resolves `opa-wasmtime` + `wasmtime`, no `wasmer`.
- Human approval recorded for `opa-wasmtime` (or a documented fallback chosen).
</verification>

<success_criteria>
- The egress-allowlist principle is authored directly as Rego, passes `opa test`, and compiles to a WASM bundle (D-02 / D-06).
- `WasmPolicyEngine` evaluates allow/deny behind the `PolicyEngine` interface, loaded once at startup (POL-03 / D-05).
- The `opa-wasmtime` substitution is human-verified before lock-in (RESEARCH A1); the interface keeps the engine a one-file swap.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton/01-04-SUMMARY.md` when done.
</output>
