"""Red-team safety harness (D-04 / SEC-01).

The Phase-1 regression-lock gate: a deterministic, hand-authored probe set that
hard-fails CI if the egress-allowlist principle stops blocking the exfil probe.
"""
