"""Persistence layer: SQLAlchemy models + engine factory.

Dialect-agnostic generic types (D-14): the same models run on the Phase-1
SQLite backend and map to PostgreSQL as the production target.
"""
